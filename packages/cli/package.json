{
  "name": "@memora/cli",
  "version": "0.1.0",
  "description": "Memora CLI — agent-first workspace signup, no email or password required",
  "type": "module",
  "bin": {
    "memora": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "pnpm build"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/memora/memora.git",
    "directory": "packages/cli"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=18"
  }
}
